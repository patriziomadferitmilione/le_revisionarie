{
  "name": "le-revisionarie",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "git add dist -f && git commit -m 'Adding dist debug' && git subtree push --prefix dist origin gh-pages",
    "daje": "git add . && git commit -m 'daje' && git push -u origin main"
  },
  "dependencies": {
    "@mdi/font": "^7.3.67",
    "font-awesome": "^4.7.0",
    "vue": "^3.3.4",
    "vue-router": "^4.2.5",
    "wave-ui": "^3.7.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.2.3",
    "gh-pages": "^6.0.0",
    "vite": "^4.4.5"
  }
}
